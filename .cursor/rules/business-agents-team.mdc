---
description: Business team agents (SKU research, marketing content, ship evaluator, marketing director) — no standalone API; Cursor/Claude execute roles and commit JSON artifacts.
globs:
  - business-agents/**
  - docs/business-agents/**
  - tools/marketing-director/**
---

# Business team agents (no API keys)

Four **teammate roles** — not external microservices. **You** (Cursor Cloud or Claude Code) read the system prompt in `docs/business-agents/prompts/` and **write validated JSON** into `business-agents/`.

| Role | Prompt file | Output |
|------|-------------|--------|
| **SKU research** (Planner input) | `prompts/sku-research-system.md` | `business-agents/sku-briefs/<id>/brief.json` |
| **Content** (Marketing draft) | `prompts/content-marketing-system.md` | `business-agents/content-drafts/*.json` |
| **Ship evaluator** (RELEASE gate) | `prompts/ship-evaluator-system.md` | `business-agents/ship-evaluations/*-eval.json` |
| **Marketing Director** (supervisor) | `prompts/marketing-director-system.md` | `business-agents/marketing-director/runs/<slug>/` |

## Workflow

1. Run scaffold/validate only via CLI: `python3 scripts/business-agents/business_agent.py …`
2. **Do not** call Anthropic/OpenAI from scripts or CI for these roles.
3. After writing JSON, run `validate` subcommand; fix until OK.
4. SKU brief: set `human_approved: true` only when the owner confirms in PR/issue.
5. Content: keep `metadata.publish_status` as `draft` until owner publishes.
6. Brand voice: read `docs/business-agents/skills/brand-voice/SKILL.md` for customer-facing copy.
7. Marketing plan: Director reads `docs/business-agents/skills/marketing-plan-current-quarter/SKILL.md` before campaign/content work.
8. Compliance: `compliance_agent` must use `docs/business-agents/skills/prohibited-claims-and-disclaimers/SKILL.md`; HIGH/CRITICAL block publish.
9. Evaluator: default `release_decision` **RELEASE BLOCKED** unless evidence supports ACCEPT.

Read [docs/business-agents/TEAM_ROLES.md](../docs/business-agents/TEAM_ROLES.md) for WO prefixes and handoffs to factory agents.

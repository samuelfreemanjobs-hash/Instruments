# Marketing Director runs

Scaffold with:

`python3 scripts/business-agents/business_agent.py director plan --request "…" --slug my-run`

Each run is a self-contained folder for director + specialist JSON artifacts.

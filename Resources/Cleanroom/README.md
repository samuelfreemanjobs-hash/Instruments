# Factory ROM

`jdupg_cleanroom.rom` is produced by the build (or `tools/GenerateCleanroomRom`). It is embedded in the plugin binary and may be copied here for inspection or redistribution with the instrument.

This file is **not** derived from Roland copyrighted ROM data.

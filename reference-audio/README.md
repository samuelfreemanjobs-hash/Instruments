# Shared reference audio (optional)

Drop **global** reference WAV/MP3 here for analysis during `/apc-dream`.

Per-plugin references go in `plugins/<Name>/references/` (preferred).

Part 2 walkthrough (Vinyl Noise): copy turntable/noise samples into
`plugins/VinylNoise/references/` before dreaming.

# Reference kicks

Drop `.wav` files here (your favorite phonk/trap 808s/kicks), then:

```bash
python3 profile_references.py references/
```

Re-run `render_kit.py` to print **match scores** against mean crest/ZCR.

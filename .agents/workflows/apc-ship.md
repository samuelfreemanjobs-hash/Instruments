---
description: PHASE Ship — copy VST3 to release/
---

# /apc-ship [Name]

**Skill:** `.agents/skills/ship/SKILL.md`

Copy `vst/<Name>/build/.../*.vst3` (or artefact path) to `release/<Name>/`.

Set `validation.ship_ready` true.

---
description: PHASE Test — build and smoke validation
---

# /apc-test [Name]

**Skill:** `.agents/skills/test/SKILL.md`

Run `bash bin/apc build <Name>` or cmake build. Report artifact path.

Set `validation.tests_passed` on success.

**Stop.** `Next: /apc-ship <Name>` or `/apc-debug <Name>`

---
description: Full mkII oscillator pipeline with confirmation between phases
---

# /logue-mkii-new [slug]

Run in order, **confirm with user after each phase**:

`/logue-mkii-setup` (if needed) → brief → design → scaffold → impl → eval → ship

Use individual workflow files between confirmations. Default slug required.

**Stop** after each phase unless user says continue.

---
description: logue mkII — implement Osc::process in osc.h
---

# /logue-mkii-impl [slug]

**Skill:** `.agents/skills/logue-mkii-impl/SKILL.md`

**Prerequisites:** scaffold tree exists; read `design.md` and golden `tr808_kick_phonk/osc.h`

**Outputs:**

- `osc.h` with real `process()`, `setParameter()`, note hooks
- `PORTING.md` updated; add `/* PORT_COMPLETE */` in `osc.h` when done
- `status.json` (`current_phase`: `impl`)

**Reference:** `docs/nts1-mkii-porting.md` v1 → mkII cheat sheet

**Stop.** `Next: /logue-mkii-eval <slug>`

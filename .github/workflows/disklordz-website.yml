name: Disklordz website

on:
  push:
    paths:
      - "disklordz/website/**"
      - "disklordz/daw-inbox/**"
      - "disklordz/rag/**"
      - "disklordz/scripts/**"
      - ".github/workflows/disklordz-website.yml"
  pull_request:
    paths:
      - "disklordz/website/**"
      - "disklordz/daw-inbox/**"
      - "disklordz/rag/**"
      - "disklordz/scripts/**"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: disklordz/website
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: disklordz/website/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run build
      - name: RAG corpus
        run: |
          python3 ../rag/scripts/chunk_corpus.py
          python3 ../rag/scripts/query_local.py "disklordz preset" | head -n 2
      - run: npm ci
        working-directory: disklordz/daw-inbox
      - run: node --check bin/watch.mjs
        working-directory: disklordz/daw-inbox

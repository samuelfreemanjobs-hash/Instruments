# Maestro routing

Default spawn for a vintage batch:

1. `market_intel` → confirm `GET /research/status`
2. `aar` if new opportunity needs greenlight
3. `artist_dl00x` for active lane
4. `vintage_keys` + `rhythm_section` + `vocal_chops` in parallel after brief approved
5. Workboard `workboard_dispatch`

OpenClaw agent folder alias: `DISKLORDZ_FACTORY_ORCHESTRATOR`.

# Template Synth (VSTi boilerplate)

**Copy this folder — do not ship products from `template/` directly.**

Use:

```bash
./tools/new-vst.sh MyPluginName
```

See `RENAME_CHECKLIST.md` after copying.
